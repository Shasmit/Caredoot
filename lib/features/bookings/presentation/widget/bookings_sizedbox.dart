import 'package:flutter/material.dart';

class BookingsSizedBox extends StatelessWidget {
  const BookingsSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
